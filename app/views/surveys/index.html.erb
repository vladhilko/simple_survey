<div class="flex-1 max-w-7xl mx-auto p-8">
  <%= render 'header' %>

  <div class="flex flex-wrap -mx-4">
    <%= render partial: 'survey_card', collection: @surveys, as: :survey  %>
  </div>

  <div class="mt-4 text-center">
    <%= paginate @surveys %>
  </div>
</div>
